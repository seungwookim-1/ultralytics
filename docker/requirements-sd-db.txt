# Docker-specific requirements without SD_db_Library
# These are the base requirements needed for the Docker build

# Core dependencies for SD_db_Library integration
PyMySQL>=1.0.0
python-dotenv>=0.19.0

# Additional dependencies that might be needed
sqlalchemy>=1.4.0
pandas>=1.1.4

# Fix sympy version conflict with onnxslim
sympy>=1.13.3
